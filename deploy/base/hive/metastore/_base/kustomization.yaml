apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared metastore HelmRelease; WRITE identity is the concrete default (HIVE_METASTORE_USER
# hive, S3_ACCESS_KEY ${s3_lake_writer}, envFrom hive-metastore-secret). readonly overlay is
# the only diff. Built standalone by validate-deploy, so valid alone.
resources:
  - helmrelease.yaml
