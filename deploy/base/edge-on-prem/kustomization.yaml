apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# on-prem ingress edge (ADR 0035), wired by modes/on-prem/edge.yaml: the oauth2-proxy
# Traefik forwardAuth Middlewares (Traefik CRDs aws has no controller for). Counterpart: ../edge-aws.
resources:
  - oauth2-proxy-middlewares.yaml
