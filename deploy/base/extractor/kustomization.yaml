apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - resources.yaml
  # Per-mode edge ConfigMaps (both modes; each HR selects one via service_mode).
  - edge.yaml
  # Ingest-DB auth variants (both ship; each HR selects one via extractor_db_auth).
  - db-auth.yaml
