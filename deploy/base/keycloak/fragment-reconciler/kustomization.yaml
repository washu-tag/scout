apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No `namespace:` transformer: the chart ships cluster-scoped RBAC, and the
# transformer would rewrite the ClusterRoleBinding's subject namespace.
resources:
  - resources.yaml
