apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ${scout_analytics_namespace} resolved by Flux post-build substitution.
namespace: '${scout_analytics_namespace}'
resources:
  - resources.yaml
  # Per-mode ingress edge (HR selects one via service_mode).
  - edge.yaml
