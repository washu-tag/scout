version: 2
updates:
  - package-ecosystem: 'github-actions'
    directories:
      - '/'
      - '.github/actions/**'
    schedule:
      interval: 'weekly'
    # Let a freshly published version age before Dependabot proposes it, to reduce
    # exposure to a compromised release (security updates ignore the cooldown).
    cooldown:
      default-days: 7
    groups:
      github-actions:
        patterns:
          - '*'

  # Gradle: security PRs only (open-pull-requests-limit: 0 disables routine
  # version updates but still allows security updates), per ADR 0015.
  # Requires resolved deps in the GitHub dependency graph — see
  # .github/workflows/gradle-dependency-submission.yaml.
  - package-ecosystem: 'gradle'
    directory: '/extractor/hl7log-extractor'
    schedule:
      interval: 'weekly'
    cooldown:
      default-days: 7
    open-pull-requests-limit: 0

  - package-ecosystem: 'gradle'
    directory: '/keycloak/event-listener'
    schedule:
      interval: 'weekly'
    cooldown:
      default-days: 7
    open-pull-requests-limit: 0

  - package-ecosystem: 'pip'
    directory: '/report-viewer'
    schedule:
      interval: 'weekly'
    open-pull-requests-limit: 0

  - package-ecosystem: 'npm'
    directory: '/report-viewer/frontend'
    schedule:
      interval: 'weekly'
    open-pull-requests-limit: 0
