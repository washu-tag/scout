name: Test

on:
  push:

jobs:
  lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4.2.1
        with:
          lfs: true
      - name: test
        shell: bash
        run: md5sum tests/staging_test_data/dicom/experiment.dcm