"""Scout report-viewer service.

Surfaces saved searches over Scout reports outside Open WebUI so the
chat surface stays light and the iframe viewer is a thin client over
a real API.
"""

__version__ = "0.0.dev0"
