# Mirror of pyproject.toml deps for the test-mode source-mount init container.
# tzlocal<5 is the one divergence: 5.x wheels don't package under `pip --target`.
fastapi<0.137
uvicorn[standard]
httpx
psycopg[binary,pool]
pydantic>=2
pydantic-settings>=2
trino
tzlocal<5
python-jose[cryptography]
prometheus-fastapi-instrumentator>=7
prometheus-client>=0.20
