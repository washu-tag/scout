"""Scout datasets service.

Stores cohort/query results outside of Open WebUI so the chat surface
stays light and the iframe viewer is a thin client over a real API.
See docs/internal/datasets-service-plan.md for the full picture.
"""

__version__ = "0.0.dev0"
