---
# OPA role defaults
#
# OPA enforces Trino's access-control policy (see policy/trino/main.rego).
# Deployed as a Deployment + Service in the same namespace as Trino so the
# query path stays in-cluster and the scout-opa chart's NetworkPolicy
# (templates/networkpolicy.yaml) admits only the Trino coordinator.

# opa_namespace is defined in scout_common/defaults/main.yaml so consumer
# roles (grafana decision-log dashboard, etc.) can reference it without
# loading the opa role.

# Image tag pinned in group_vars/all/versions.yaml.
opa_image: openpolicyagent/opa

# Resource sizing — OPA is CPU-bound on policy evaluation, memory-bound on
# the policy + decision log buffer. 2 replicas for HA.
opa_replicas: 2
opa_cpu_request: 50m
opa_cpu_limit: 500m
opa_memory_request: 64Mi
opa_memory_limit: 256Mi

# === Data document (rendered into /policies/data.json alongside the Rego)

# Trino service principals that own SECURITY DEFINER views. When an
# invoker queries a view, Trino re-evaluates the view's underlying
# reads against the view OWNER's identity — both /allow and /rowFilters
# /batchColumnMasks. If the owner has restrictive AuthZ attributes (or
# none, like the default `trino` admin user), the underlying reads get
# clamped to zero rows, breaking the view for every invoker.
#
# Owners listed here bypass row filters and column masks so the view
# can materialize unrestricted; the invoker's AuthZ is still applied at
# the view level via filtered_tables (the views ARE listed above).
# Defaults to the Trino password-auth admin (`trino`), which is what
# hl7-transformer connects as when running CREATE VIEW.
trino_view_owner_principals:
  - trino

# Scout data tables that receive row-filter enforcement. Every attribute
# in trino_attribute_filters scopes to this list by default — adding a
# table here turns on row filters for that table across all configured
# attributes. Tables NOT on this list (information_schema, system, the
# patient mapping table, etc.) receive no row filter.
#
# Prefix-match (table_prefix) covers the entire `reports*` family in
# one entry: the base report_delta_table_name table plus every
# derivative the hl7-transformer creates (reports_curated,
# reports_latest, reports_dx, all *_epic_view joined views). New
# derivatives sharing the prefix get filtered automatically -- no
# inventory edit per new table -- as long as they carry the
# trino_attribute_filters columns. Tables matched by the prefix but
# also listed in trino_hidden_tables (e.g. the patient mapping
# tables) are explicitly excluded from row-filter scoping by the
# rego, so the prefix over-matching them is safe.
#
# Per-attribute overrides are supported via the optional `tables` key
# on an attribute_filters entry (see below) — useful when a column
# only exists on a subset of these tables.
trino_filtered_tables:
  - { catalog: delta, schema: default, table_prefix: '{{ report_delta_table_name }}' }

# Additional tables denied for direct access. Users querying these by
# name get a permission error, and the tables are hidden from SHOW
# TABLES output. The patient mapping pair written by the hl7-transformer
# (<report_delta_table_name>_report_patient_mapping + _history) is always
# hidden as a baseline: the deploy task renders those derived names into
# data.baseline_hidden_tables from report_delta_table_name (so a renamed
# report table can't leave the mapping table unhidden), and
# policy/trino/main.rego falls back to the stock `reports` names if that
# data key is absent. That's intrinsic to Scout's data lake shape and
# can't be safely removed. This list is unioned on top for site-specific
# cross-facility join tables.
#
# Intended for join-target tables where the row filters can't reach
# (e.g., tables keyed on scout_patient_id rather than sending_facility).
# `_epic_view`-style join views over these tables keep working because
# they're created SECURITY DEFINER (view's underlying reads aren't
# re-checked against the invoker's permissions). Verify with
# `SHOW CREATE VIEW <name>` before adding a new entry whose views
# need to keep working.
trino_hidden_tables: []

# `trino_attribute_filters` lives in scout_common/defaults/main.yaml —
# it's the single source of truth shared by the keycloak role (renders
# user-profile attribute editors) and this role (renders the OPA data
# document). Override in inventory.yaml to add restriction dimensions.

# Columns the policy redacts when the calling user's mask_phi_fields
# attribute is unset or "true". Mask expression depends on column type:
# varchar columns get the literal '[REDACTED]' (visible to the user);
# anything else gets `NULL` (Trino's expression analyzer coerces the
# unknown-typed null to the column's declared type). Adding columns is
# an inventory edit — no policy change needed.
#
# full_patient_name is the structured-array twin of patient_name, so
# masking both prevents recovery of the redacted simple name through
# the typed twin.
trino_masked_columns:
  - patient_name
  - full_patient_name
  - zip_or_postal_code

# MinIO bundle endpoint (ADR 0021). OPA's bundle plugin pulls
# the object at <opa_bundles_bucket>/<opa_bundle_object_key> from MinIO
# every 5–10s; the bundle is published by the Keycloak SPI listener
# (OpaUserBundlePublisherProvider).
# Headless service explicitly chosen so DNS resolves to all MinIO pods —
# Helm tenant's primary Service ("minio.<ns>") isn't named consistently
# across our environments, but "-hl" is set by the operator.
opa_bundle_minio_url: 'http://{{ minio_tenant_name }}-hl.{{ minio_tenant_namespace }}:9000'

# Bundle pull cadence. Lower bound on staleness for a Keycloak admin
# action propagating to a Trino decision. Comfortably inside ADR 0020's
# "user disabled at their partner institution must lose access within
# seconds" requirement; tune downward at proportional MinIO request-rate
# cost if a deployment needs tighter convergence.
opa_bundle_min_delay_seconds: 5
opa_bundle_max_delay_seconds: 10

# Decision logs go to stdout (Loki picks them up via existing Scout log shipping).
opa_decision_log_min_delay_seconds: 5
opa_decision_log_max_delay_seconds: 15
