---
# Minimal galaxy_info to satisfy Molecule validation
# Not published to Galaxy - this is just to pass checks
galaxy_info:
  role_name: harbor
  namespace: washu_scout

# Role dependencies
dependencies:
  - role: scout_common
