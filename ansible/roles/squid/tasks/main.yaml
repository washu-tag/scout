---
# Main entry point for squid role
# Delegates to specific task files based on tags
# Note: air_gapped guard is not needed here because this role is only
# included from the staging playbook, which already gates on air_gapped.

- name: Include deployment tasks
  ansible.builtin.include_tasks: deploy.yaml
  tags:
    - squid
    - deploy
