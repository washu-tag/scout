---
# Main entry point for the xnat role.
# Deploys XNAT (xnatworks/xnat-web) + plugins behind oauth2-proxy, with the
# off-the-shelf xnat-openid-auth-plugin for Keycloak SSO.

- name: Include deployment tasks
  ansible.builtin.include_tasks: deploy.yaml
