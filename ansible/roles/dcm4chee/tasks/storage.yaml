---
# Node-side storage directory creation
# This task runs on k3s_cluster nodes to create directories for persistent volumes

- name: Create storage directories
  ansible.builtin.include_role:
    name: scout_common
    tasks_from: storage_dir
  vars:
    storage_definitions: '{{ dcm4chee_storage_definitions }}'
