---
# Main entry point for hl7-listener role
# Deploys Strimzi Kafka and the HL7 listener/batcher Deployments (CI-built image from GHCR)

- name: Include deployment tasks
  ansible.builtin.include_tasks: deploy.yaml
