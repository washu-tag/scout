---
dependency:
  name: galaxy
driver:
  name: default
platforms:
  - name: localhost
    groups:
      - localhost
provisioner:
  name: ansible
  inventory:
    host_vars:
      localhost:
        ansible_connection: local
verifier:
  name: ansible
