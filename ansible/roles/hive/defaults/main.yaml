---
# Default values for hive-metastore deployment

# Resource configuration (dev defaults - CI friendly)
hive_resources:
  requests:
    cpu: 100m
    memory: 1Gi
  limits:
    cpu: 2
    memory: 2Gi
