---
- name: Install components for Analytics service
  hosts: server
  gather_facts: false
  environment: '{{ cluster_k8s_environment }}'
  roles:
    - role: opa
    - role: trino
    - role: superset
