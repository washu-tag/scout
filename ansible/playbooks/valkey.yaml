---
# Playbook to deploy Valkey for session storage and caching
# This deploys a standalone Valkey instance shared by all Scout services
# Storage is dynamically provisioned using the default storage class (local-path)

- name: Deploy Valkey
  hosts: server
  gather_facts: false
  environment: '{{ cluster_k8s_environment }}'
  roles:
    - role: valkey
