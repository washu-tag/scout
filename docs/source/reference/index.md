# Reference

Reference material shared across audiences — the report data schema, Scout's
backend architecture, and supporting database schemas.

```{note}
Docs reorganization in progress (issue #468). The pages below still live at
their old locations; a later step physically moves them under `reference/`.
`services` is a mixed page that will be split — its user-facing service
descriptions move to the Quickstart, and only the architecture/backend content
stays here.
```

```{toctree}
:maxdepth: 1
/dataschema
/services
/technical/ingest_status_db
```
