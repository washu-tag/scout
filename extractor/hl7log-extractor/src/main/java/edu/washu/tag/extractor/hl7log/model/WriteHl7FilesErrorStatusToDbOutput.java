package edu.washu.tag.extractor.hl7log.model;

/**
 * Output for the activity that writes the error status of HL7 files to the database.
 * Currently empty, but included in case future outputs are needed.
 */
public record WriteHl7FilesErrorStatusToDbOutput() {}
