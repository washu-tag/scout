apiVersion: v1
kind: Service
metadata:
  name: {{ include "report-viewer.fullname" . }}
  labels:
    {{- include "report-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "report-viewer.selectorLabels" . | nindent 4 }}
