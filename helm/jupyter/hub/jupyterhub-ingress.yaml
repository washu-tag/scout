spec:
  ingressClassName: traefik
  rules:
    - host: ${FQDN}
      http:
        paths:
          - backend:
              service:
                name: proxy-public
                port:
                  name: http
            path: /jupyter
            pathType: Prefix