proxy:
  service:
    type: ClusterIP

hub:
  baseUrl: /jupyter
  shutdownOnLogout: true

ingress:
  enabled: true
  hosts:
    - ${FQDN}

singleuser:
  image:
    name: local/pyspark-notebook
    tag: latest
    pullPolicy: Never
  cmd: null

prePuller:
  hook:
    enabled: false
