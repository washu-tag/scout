apiVersion: v2
name: scout-dashboards
description: Imports Scout's Superset dashboards, charts, and datasets from analytics YAML files. Re-runs whenever any analytics file changes (via checksum/config annotation).
type: application
version: 0.0.0-dev
appVersion: "latest"
