{{ .Chart.Name }} deployed.

Enabled dashboard families:
{{- range $family, $cfg := .Values.dashboards }}
{{- if $cfg.enabled }}
  - {{ $family }}
{{- end }}
{{- end }}

The import Job runs once per Helm install/upgrade. Watch progress with:

    kubectl logs -n {{ .Release.Namespace }} \
      -l app.kubernetes.io/instance={{ .Release.Name }},component=dashboard-import \
      --tail=200 -f

Whenever any enabled analytics file changes — or a family toggle flips — the
rendered ConfigMap's sha256sum changes, the import Job pod template's
checksum/config annotation changes, and Helm replaces the Job on the next
`helm upgrade`. No Ansible glue required.
