apiVersion: v1
kind: Service
metadata:
  name: {{ include "datasets-service.fullname" . }}
  labels:
    {{- include "datasets-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datasets-service.selectorLabels" . | nindent 4 }}
