apiVersion: v1
kind: Service
metadata:
  name: {{ include "report-viewer-service.fullname" . }}
  labels:
    {{- include "report-viewer-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "report-viewer-service.selectorLabels" . | nindent 4 }}
