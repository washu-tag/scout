apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "log-extractor.fullname" . }}-config
  labels:
    {{- include "log-extractor.labels" . | nindent 4 }}
data:
  application.yaml: |
    {{- .Values.config.application | toYaml | nindent 4 }}
