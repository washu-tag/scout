apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "dcm4chee.keycloak.labels" . | nindent 4 }}
  name: keycloak
spec:
  ports:
    - name: https
      port: 8843
  selector:
    dcm4chee-component: keycloak
