apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "dcm4chee.mariadb.labels" . | nindent 4 }}
  name: mariadb
spec:
  ports:
    - port: 3306
  selector:
    dcm4chee-component: mariadb
