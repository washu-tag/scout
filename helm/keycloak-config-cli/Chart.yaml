apiVersion: v2
name: keycloak-config-cli
description: Import JSON-formatted configuration files into Keycloak - Configuration as Code for Keycloak.
home: https://github.com/adorsys/keycloak-config-cli

type: application

version: 0.0.0-dev

# Stamped at package time from versions.yaml (keycloak_config_cli_image_tag) by .github/scripts/chart-app-version.sh;
# this placeholder never ships and deliberately does not resolve to an image.
appVersion: "0.0.0-dev"
