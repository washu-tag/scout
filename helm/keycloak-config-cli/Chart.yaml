apiVersion: v2
name: keycloak-config-cli
description: Render the Scout base realm and publish it for the app manager to apply.
home: https://github.com/adorsys/keycloak-config-cli

type: application

version: 0.0.0-dev
