{{- if .Values.serviceAccount.create }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "appmanager.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "appmanager.labels" . | nindent 4 }}
{{- end }}
