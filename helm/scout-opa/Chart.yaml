apiVersion: v2
name: scout-opa
description: OPA policy engine enforcing Scout's Trino access-control policy (ADRs 0020/0021)
type: application
version: 0.0.0-dev
# The OPA version this chart deploys (image.tag default); kept concrete so no
# published chart ships appVersion: latest.
appVersion: "1.16.2"
