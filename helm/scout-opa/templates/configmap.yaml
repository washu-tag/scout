apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "scout-opa.fullname" . }}-policy
  labels:
    {{- include "scout-opa.labels" . | nindent 4 }}
data:
  policy.rego: |
{{ .Values.policy.rego | indent 4 }}
  data.json: |
{{ .Values.data.json | indent 4 }}
  config.yaml: |
{{ .Values.config.yaml | indent 4 }}
