spring:
  application:
    name: temporal-java
  temporal:
    connection: "temporal-frontend.temporal:7233"
    workers-auto-discovery:
      packages:
        - edu.washu.tag.temporal.activity
        - edu.washu.tag.temporal.workflow
